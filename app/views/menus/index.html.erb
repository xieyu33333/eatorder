<!-- <h1>Listing menus</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Faverate</th>
    <th>All count</th>
    <th>Day count</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @menus.each do |menu| %>
  <tr>
    <td><%= menu.name %></td>
    <td><%= menu.price %></td>
    <td><%= menu.faverate %></td>
    <td><%= menu.all_count %></td>
    <td><%= menu.day_count %></td>
    <td><%= link_to 'Show', menu %></td>
    <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
    <td><%= link_to 'Destroy', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu', new_menu_path %>
 -->